@model WebApplication1.Models.Book

@{
    ViewBag.Title = "Random";
    Layout = "/Views/Shared/_Layout.cshtml";
    var book = ViewBag["Book"] as Book;
    // we have to make link to things we want "viewbaged somewhere"
}

<h2>@Model.Name</h2>